@model MvcAccount.Account.IndexViewModel
@section head {
<title>@Model.Title</title>
}
<div class="@Model.ViewCssClass">
   <h1>@Model.Title</h1>
   <ul>
      @foreach (var item in Model.Links) {
         <li>@item</li>
      }
   </ul>
</div>