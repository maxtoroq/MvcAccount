@model MvcAccount.Password.Reset.VerificationMessageViewModel
Resetoidaksesi salasanan tiliin @Html.Raw(Model.SiteName) @Html.Raw(Model.Username), 
ole hyvä ja seuraa alla olevaa linkkiä:

@Html.Raw(Model.Url)

Jos linkki ei toimi, kopioi osoite ja avaa se uudessa selainikkunassa.
 
Jos et ole pyytänyt tätä muutosta, joku muu on voinut tehdä sen käyttäen sähköpostiosoitettasi. 
Tässä tapauksessa sinun ei tarvitse tehdä mitään, vaan voit jättää tämän viestin huomiotta.

Tämä on automaattinen viesti. Tähän sähköpostiin tulevia viestejä ei 
ei lueta eikä niihin vastata.