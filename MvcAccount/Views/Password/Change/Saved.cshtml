@using HtmlUtility = MvcAccount.Html.HtmlUtility;
@model MvcAccount.Password.Change.SavedViewModel
@section head {
<title>@Model.Title</title>
}
<div class="password-change-saved">
   <h1>@Model.Title</h1>
   <p>@Model.Message</p>
   <p>
      @HtmlUtility.Link(Model.NextLink)
   </p>
</div>