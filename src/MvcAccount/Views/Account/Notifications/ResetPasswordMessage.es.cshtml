@model MvcAccount.ViewModels.ResetPasswordMessageViewModel
Para iniciar el proceso de restablecimiento de la contraseña de su cuenta 
@Html.Raw(Model.Username) en @Html.Raw(Model.SiteName), visite el vínculo siguiente:

@Html.Raw(Model.Url)

Si el vínculo anterior no funciona, pruebe a copiarlo y pegarlo en una nueva 
ventana del navegador.

Si recibió este mensaje por error es probable que otro usuario haya ingresado
su nombre de usuario o correo electrónico al intentar restablecer una contraseña. 
Si no inicio este requerimiento no es necesario realizar ninguna acción y puede 
ignorar ese mensaje.

La función de este mensaje es únicamente informativa. Las respuestas no se 
supervisarán ni se contestarán.