@model MvcAccount.ViewModels.PasswordSavedViewModel
@section head {
<title>@Model.Title</title>
}
<div class="password-saved">
   <h1>@Model.Title</h1>
   <p>@Model.Message</p>
   <p>
      @MvcAccount.HtmlUtility.Link(Model.NextLink)
   </p>
</div>